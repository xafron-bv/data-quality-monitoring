{
  "description": "Generate precision-recall and ROC curves for ML-based and LLM-based anomaly detection",
  "arguments": {
    "data_file": {
      "type": "positional",
      "help": "Path to the CSV data file",
      "required": true
    },
    "detection_type": {
      "type": "string",
      "help": "Type of detection to evaluate",
      "choices": ["ml", "llm"],
      "default": "ml"
    },
    "fields": {
      "type": "list",
      "help": "Specific fields to generate curves for (default: all available)",
      "default": null
    },
    "output_dir": {
      "type": "string",
      "help": "Output directory for curves",
      "default": "detection_curves"
    },
    "thresholds": {
      "type": "list",
      "item_type": "float",
      "help": "Specific thresholds to test (default: ML=0.1-0.95, LLM=-0.5-0.1)",
      "default": null
    },
    "brand": {
      "type": "string",
      "help": "Brand name (deprecated - uses static config)",
      "default": null
    }
  }
}